{
  "name": "core-skills",
  "version": "0.9.0",
  "description": "Cross-stack skills. docs-sync catches code/doc drift at end-of-task. verify-changes is the canonical audit engine — accepts a structured brief (scope + dimensions + depth + fix) from stack-pack thin-wrappers, owns iteration, batching, and the fix loop. subagent-brief enforces warm-brief discipline when delegating. Pure prompts — no hooks.",
  "author": {
    "name": "pixelcrafts",
    "url": "https://github.com/pixelcrafts-app"
  },
  "homepage": "https://github.com/pixelcrafts-app/claude-craft",
  "license": "MIT",
  "keywords": ["skills", "cross-stack", "docs-sync", "verify-changes", "subagent-brief"]
}
